# 폰에서 테스트하기

## 왜 그냥 파일을 못 여나

파일을 폰에서 직접 열면 **GPS가 작동하지 않습니다.** 브라우저 보안 규칙이라
어떤 방법으로도 우회가 안 됩니다. 웹 주소(https)로 열어야 합니다.

아래 3분이면 끝납니다.

---

## 1단계 — 올리기 (컴퓨터에서, 3분)

1. 받은 `dist` 폴더를 통째로 저장합니다
   - `index.html`, `data.json`, `manifest.json`, `sw.js`
   - `roads/` 폴더 (도로망 타일 11,569개, 49MB)
2. **파일이 많아 GitHub Pages 를 권합니다** (아래 자동 갱신 항목 참고).
   Netlify Drop 도 되지만 파일 1만 개 업로드는 오래 걸립니다.
   도로망 없이 먼저 써보시려면 `roads/` 폴더를 빼고 올려도 됩니다.
   그러면 설정의 "도로 매칭"이 자동으로 꺼진 것처럼 동작합니다.
3. 브라우저에서 **app.netlify.com/drop** 접속
4. `dist` **폴더를 통째로** 드래그해서 올립니다
5. 잠시 뒤 `무작위이름.netlify.app` 주소가 나옵니다

> 파일을 각각 올리면 안 됩니다. 폴더째 올려야 서로를 찾습니다.
> 앱 본체는 41KB, 카메라 데이터는 865KB로 분리돼 있습니다.
> 데이터를 갱신할 때는 `data.json` 하나만 바꾸면 됩니다.

---

## 2단계 — 폰에서 열기

1. 그 주소를 폰으로 보냅니다 (카톡 나에게 보내기 등)
2. 폰에서 링크를 엽니다
3. **홈 화면에 추가**합니다
   - 아이폰(사파리): 하단 공유 버튼 → "홈 화면에 추가"
   - 안드로이드(크롬): 우측 상단 점 3개 → "홈 화면에 추가"

홈 화면 아이콘으로 실행하면 주소창이 사라져 화면이 넓어지고,
한 번 연 뒤에는 인터넷이 끊겨도 열립니다.

---

## 3단계 — 차 타기 전 점검 (집에서)

앱 첫 화면의 두 버튼으로 확인합니다.

### 진단 버튼

여섯 줄이 나옵니다. 전부 초록색 OK면 준비 끝입니다.

| 항목 | 안 될 때 |
|---|---|
| 보안 연결(https) | 파일을 직접 연 상태입니다. 웹 주소로 여세요 |
| 위치 기능 | 브라우저를 바꿔보세요 (크롬/사파리 권장) |
| 음성 합성 | 폰 볼륨과 무음 스위치를 확인하세요 |
| 한국어 음성 | 노란색이어도 실제로는 나올 수 있습니다. 아래 모의주행으로 확인 |
| 화면 항상 켜기 | 노란색이면 폰 설정에서 화면 자동잠금을 5분 이상으로 |
| GPS 수신 | 실내면 오차가 큽니다. 창가나 밖에서 다시 |

### 모의주행 버튼

**차 없이 앱 전체를 검증합니다.** 울산 이예로 24km 실제 좌표를 따라
시속 80km로 달리는 상황을 재생합니다.

확인할 것:
- **지도에 파란 점(내 위치)과 자취**가 그려지는지
- 카메라 목록이 뜨고 거리가 **줄어드는지**
- 지나친 카메라가 목록에서 **사라지는지**
- 음성이 나오는지 ("560미터 앞 단속 카메라, 제한 80" 같은 안내)
- 구간단속 구간에서 파란 막대와 평균속도가 뜨는지

여기까지 되면 실차에서도 됩니다.

---

## 4단계 — 실차 테스트

### 출발 전
- [ ] 거치대에 고정 (손에 들지 마세요)
- [ ] 충전 케이블 연결 — GPS 상시 사용이라 배터리가 빨리 닳습니다
- [ ] 앱 열고 **시작** 누르기
- [ ] 위치 권한 "앱 사용 중 허용" 선택
- [ ] 볼륨 확인

### 달리면서 볼 것

**운전 중에는 화면을 보지 마세요.** 신호 대기나 휴게소에서 확인하세요.

1. **속도**가 계기판과 비슷한가 (GPS 속도는 보통 2~3km/h 낮게 나옵니다)
2. **지도의 내 위치가 실제 도로 위에 있는가** ← 위치 인식 확인의 핵심
3. **직진/좌커브/우커브** 표시가 실제와 맞는가
4. **거리가 줄어드는가**
5. **실제 카메라 위치와 맞는가** — 이게 제일 중요합니다
6. 없는 카메라를 알리지는 않는가
7. **반대 차로 카메라를 알리지 않는가**

### 알려주실 것

문제가 있으면 이렇게 알려주시면 바로 고칩니다.

- 어느 도로, 대략 어디쯤
- 화면에 뭐라고 떴는지 (캡처가 제일 좋습니다)
- 실제로는 어땠는지

---

## 미리 알아두실 것

**고속도로에서 화면이 자주 빕니다.** 공공데이터의 고속도로 등록이 지역마다
들쭉날쭉합니다. 울산경찰청은 고속국도 카메라를 **0건** 등록했습니다.
부산울산고속도로 데이터가 아예 없습니다. 앱 문제가 아닙니다.

**이동식 단속(삼각대)은 없습니다.** 표준데이터는 고정식만 포함합니다.

**이 앱은 참고용입니다.** 항상 제한속도를 지키고 도로 표지를 우선하세요.

---

## 설정 항목

| 항목 | 기본 | 설명 |
|---|---|---|
| 탐지 거리 | 10km | 전방 몇 km까지 볼지 |
| 음성 안내 | 켬 | |
| 지도 표시 | 켬 | 끄면 목록이 더 넓게 보입니다 |
| 지도 배경 (인터넷 사용) | 끔 | 켜면 실제 지도 그림이 깔립니다 |
| 반대 차로 걸러내기 | 켬 | 끄면 반대편 카메라도 나옵니다 |
| 신호위반 카메라 표시 | 켬 | 화면 표시 여부 |
| 신호위반도 음성 안내 | 끔 | 켜면 시내에서 많이 울립니다 |
| 속도로 도로 자동 판별 | 켬 | 끄면 주변 모든 카메라가 나옵니다 |
| 커브 따라가기 | 켬 | 이상하면 꺼보세요 |
| 도로 매칭 (정밀) | 켬 | 실제 도로망으로 내 길을 특정합니다 |
| 화면 항상 켜기 | 켬 | |

---

## 지도 보는 법

화면 위쪽 작은 지도는 **길찾기용이 아니라 GPS 확인용**입니다.

| 표시 | 뜻 |
|---|---|
| 흰 동그라미 + 파란 삼각형 | 내 위치와 진행 방향 |
| 옅은 파란 원 | 위치 오차 범위. 클수록 GPS가 부정확한 것 |
| 파란 선 | 지나온 자취 |
| **굵은 파란 선** | **매칭된 실제 도로** (도로 데이터가 있을 때) |
| 점선 + 옅은 띠 | 경로 추정 (도로 데이터가 없을 때) |
| **큰 빨간 점** | **내 도로 위 카메라** (거리 표시) |
| 작고 흐린 점 | 옆길 카메라 |
| 주황 점 | 구간단속 |
| 파란 점 | 알림 대상 카메라 |
| 회색 점 | 옆길일 수 있는 카메라 (음성 없음) |
| 주황 점 | 구간단속 시점·종점 |
| 좌측 +/− | 확대·축소 |

**지도 배경은 기본으로 꺼져 있습니다.** 인터넷을 쓰지 않고, 격자와 자취만
그립니다. 자취가 매끄럽게 이어지면 GPS가 정상입니다.

실제 도로 위에 내가 찍히는지 확인하려면 설정에서 **지도 배경**을 켜세요.
데이터를 조금 쓰지만(한 화면에 타일 9장) 훨씬 확실합니다. 이 배경은
OpenStreetMap 자료이며, 저장공간이 차지 않도록 캐시하지 않습니다.

좌하단에 **"도로 매칭됨"** 이 뜨면 실제 도로 위에 정확히 붙은 상태입니다.
**"경로 추정"** 이면 도로 데이터가 없는 구역이라 근사 방식으로 동작합니다.

**점선이 실제 도로를 따라가야 정상입니다.** 점선이 도로에서 벗어나 있으면
앱이 진행방향을 잘못 잡은 것이고, 그러면 엉뚱한 카메라가 뜹니다.
이럴 때 캡처해서 알려주시면 바로 원인을 찾을 수 있습니다.

**정상 판정 기준**
- 오차 20m 이하 (표시가 지도 좌하단에 나옵니다)
- 자취가 도로를 따라 매끄럽게 이어짐
- 내 위치가 실제 차로에서 크게 벗어나지 않음

터널이나 고층 건물 사이에서 오차가 커지는 건 정상입니다.

---

## 데이터 갱신

표준데이터는 **매월 초** 전국 단위로 병합 갱신됩니다.

### 수동 (5분)

```bash
python3 update_data.py --csv 새로받은파일.csv
```

받기 → 검사 → 재빌드 → 테스트까지 한 번에 돕니다.
**테스트가 하나라도 실패하면 갱신하지 않습니다.** 기존 배포본이 그대로 유지됩니다.

이상한 데이터도 걸러냅니다.
- 행 수가 3만 미만 → 거부
- 필수 컬럼 누락 (데이터 형식 변경) → 거부
- 좌표 정상 비율 90% 미만 → 거부
- 지난번과 내용이 같으면 → 건너뜀

그다음 `dist` 폴더를 다시 올리면 끝입니다.

### 자동 (GitHub 사용, 설정은 한 번만)

`.github/workflows/update.yml` 이 매월 5일 새벽 5시에 자동으로 돕니다.
받기 → 검사 → 빌드 → 테스트 → 배포까지 사람 손이 필요 없습니다.
실패하면 배포하지 않고 알림만 옵니다.

설정 순서:
1. GitHub 계정 만들기 → 저장소 생성
2. 전체 파일 업로드
3. Settings → Pages → Source 를 "GitHub Actions" 로
4. Actions 탭에서 "Run workflow" 로 한 번 수동 실행해 확인

주소가 `계정이름.github.io/저장소이름` 으로 나옵니다. Netlify 대신 여기를 써도 됩니다.

> 공공데이터포털의 파일 다운로드 주소는 수시로 바뀝니다.
> 자동 갱신이 실패하면 `update_data.py --url "실제주소"` 로 주소만 바꿔주면 됩니다.

---

## 출처 표기 (필수)

앱 첫 화면에 이미 들어가 있습니다. 지우지 마세요.

- 단속카메라 정보: 공공데이터포털 전국무인교통단속카메라표준데이터
- 도로망: 국토교통부 표준노드링크 (ITS 국가교통정보센터)
- 지도 배경: © OpenStreetMap 기여자

공공데이터는 영리 목적으로도 이용할 수 있지만 출처 표기는 필요합니다.
